version: '3.8'

services:
  api:
    build: .
    container_name: aisaraj-fastapi
    restart: always
    ports:
      - "80:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - ENVIRONMENT=production
